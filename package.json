{
  "name": "cleared-jobs",
  "private": false,
  "description": "Durable job lifecycle SDK for x402-paid async work",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "demo:happy": "bash scripts/demo-happy-path.sh",
    "demo:retry": "bash scripts/demo-duplicate-retry.sh",
    "demo:lost": "bash scripts/demo-lost-response.sh",
    "demo:timeout": "bash scripts/demo-timeout.sh"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0",
    "zod": "^3.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}
